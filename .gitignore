/target


# Linea files and personal stuff so it doesn't get sent somewhere to china
contacts/
media/
msg/
*.bin
# The .bin because your encrypted compressed chats are .bin
*.key
# I think the word "key" says it all

.idea